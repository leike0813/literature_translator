{
  "type": "object",
  "properties": {
    "target_language": {
      "type": "string",
      "default": "zh-CN",
      "description": "Target language for translation (BCP-47 code, e.g. zh-CN, en, ja)"
    },
    "mode": {
      "type": "string",
      "enum": ["fast", "high_quality"],
      "default": "fast",
      "description": "Translation mode. fast: block-level translation, no sentence splitting, no placeholder protection, relaxed quality checks. high_quality: sentence-level translation with full placeholder protection and structured review."
    }
  },
  "required": []
}
